{
  "name": "idp",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run",
    "deploy": "twilio-run deploy"
  },
  "dependencies": {
    "fast-xml-parser": "^3.17.4",
    "hbs": "^4.1.1",
    "saml": "^0.15.0",
    "xml-crypto": "^2.0.0",
    "zlib": "^1.0.5"
  },
  "devDependencies": {
    "twilio-run": "^2.6.0"
  },
  "engines": {
    "node": "10"
  }
}
